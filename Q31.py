# 31.​ Write a Python program to iterate over dictionaries using for loops.

def sampleFunction(dic):
    for key, value in dic.items():
        print(key, value)
    
sampleFunction({'apple':'testy', 'mango':'sour'})
